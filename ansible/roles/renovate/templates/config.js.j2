module.exports = {
  platform: 'forgejo',
  endpoint: '{{ forgejo_url }}/api/v1',
  baseDir: '{{ renovate_base_dir }}',
  logLevel: 'info',
{% if renovate_repositories | length > 0 %}
  repositories: {{ renovate_repositories | to_json }},
{% else %}
  autodiscover: true,
{% endif %}
};
