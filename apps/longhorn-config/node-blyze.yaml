apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: blyze
  namespace: longhorn-system
spec:
  allowScheduling: true
  disks:
    storage:
      path: /mnt/longhorn/
      allowScheduling: true
      storageReserved: 0
