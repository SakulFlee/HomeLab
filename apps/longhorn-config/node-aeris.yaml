apiVersion: longhorn.io/v1beta2
kind: Node
metadata:
  name: aeris
  namespace: longhorn-system
spec:
  name: aeris
  allowScheduling: true
  disks:
    storage:
      path: /mnt/longhorn/
      allowScheduling: true
      storageReserved: 0
