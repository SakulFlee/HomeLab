apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: zerossl-production
spec:
  acme:
    server: https://acme.zerossl.com/v2/DV90
    externalAccountBinding:
      keyID: Yd5rPVZTGWojeHuyflLYxg
      keySecretRef:
        name: zerossl-eab-secret
        key: hmac
    privateKeySecretRef:
      name: zerossl-production
    solvers:
    - http01:
        ingress:
          class: traefik
