apiVersion: batch/v1
kind: CronJob
metadata:
  name: paperless-pg-dump
  namespace: paperless
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pg-dump
              image: postgres:16-alpine
              env:
                - name: PGHOST
                  valueFrom:
                    secretKeyRef:
                      name: paperless-ngx-postgres-app
                      key: host
                - name: PGPORT
                  valueFrom:
                    secretKeyRef:
                      name: paperless-ngx-postgres-app
                      key: port
                - name: PGUSER
                  valueFrom:
                    secretKeyRef:
                      name: paperless-ngx-postgres-app
                      key: user
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: paperless-ngx-postgres-app
                      key: password
                - name: PGDATABASE
                  valueFrom:
                    secretKeyRef:
                      name: paperless-ngx-postgres-app
                      key: dbname
              command:
                - /bin/sh
                - -ec
                - |
                  DATE=$(date +%Y%m%d-%H%M%S)
                  pg_dump -Fc -f "/dumps/$PGDATABASE-$DATE.dump"
                  find /dumps -name '*.dump' -type f -mmin +2880 -delete
              volumeMounts:
                - name: dumps
                  mountPath: /dumps
          volumes:
            - name: dumps
              persistentVolumeClaim:
                claimName: paperless-pg-dumps
