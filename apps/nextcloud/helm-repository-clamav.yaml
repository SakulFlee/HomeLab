apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: clamav
  namespace: nextcloud
spec:
  interval: 5m0s
  url: oci://registry.gitlab.com/xrow-public/helm-clamav/charts/clamav
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  ref:
    semver: "^0.7.0"
