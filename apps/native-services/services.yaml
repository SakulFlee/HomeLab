apiVersion: v1
kind: Service
metadata:
  name: llama-swap
  namespace: native
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 30001
---
apiVersion: v1
kind: Service
metadata:
  name: nas
  namespace: native
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 9443