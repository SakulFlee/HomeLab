apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: native-services
  namespace: native
resources:
  - namespace.yaml
  - services.yaml
  - endpoints.yaml
  - ingresses.yaml
  - matrix-tcp.yaml
