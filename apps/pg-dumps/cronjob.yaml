apiVersion: batch/v1
kind: CronJob
metadata:
  name: pg-dump
  namespace: pg-dumps
spec:
  schedule: "0 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          containers:
            - name: pg-dump
              image: postgres:16-alpine
              env:
                - name: PGHOST
                  valueFrom:
                    secretKeyRef:
                      name: pg-dump-target
                      key: host
                - name: PGPORT
                  valueFrom:
                    secretKeyRef:
                      name: pg-dump-target
                      key: port
                - name: PGUSER
                  valueFrom:
                    secretKeyRef:
                      name: pg-dump-target
                      key: user
                - name: PGPASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: pg-dump-target
                      key: password
                - name: PGDATABASE
                  valueFrom:
                    secretKeyRef:
                      name: pg-dump-target
                      key: dbname
              command:
                - /bin/sh
                - -ec
                - |
                  DATE=$(date +%Y%m%d-%H%M%S)
                  pg_dump -Fc -f "/dumps/$PGDATABASE-$DATE.dump"
                  find /dumps -name '*.dump' -type f -mmin +2880 -delete
              volumeMounts:
                - name: dumps
                  mountPath: /dumps
          volumes:
            - name: dumps
              persistentVolumeClaim:
                claimName: pg-dumps
