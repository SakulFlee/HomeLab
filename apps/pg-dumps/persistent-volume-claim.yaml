apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-dumps
  namespace: pg-dumps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-backup
  resources:
    requests:
      storage: 10Gi
