apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: headscale
  namespace: headscale
resources:
  - namespace.yaml
  - configmap.yaml
  - data-pvc.yaml
  - statefulset.yaml
  - service.yaml
  - ingress.yaml
  - middleware-vpn-only.yaml
  - middleware-headplane-redirect.yaml
  - reconciler/