apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synapse-pg-dumps
  namespace: synapse
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-backup
  resources:
    requests:
      storage: 5Gi
