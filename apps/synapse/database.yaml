apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: postgres
  namespace: synapse
spec:
  instances: 1
  bootstrap:
    initdb:
      args:
        - --lc-collate=C
        - --lc-ctype=C
  storage:
    storageClass: local-path-persistent
    size: 5Gi
