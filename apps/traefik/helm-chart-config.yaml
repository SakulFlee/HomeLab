apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    ports:
      matrix-federation:
        port: 8448
        expose: true
        exposedPort: 8448
        protocol: TCP
