apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie
  namespace: media
spec:
  storageClassName: local-path-persistent
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
