apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: subnet-router-state
  namespace: subnet-router
spec:
  storageClassName: local-path-backup
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi