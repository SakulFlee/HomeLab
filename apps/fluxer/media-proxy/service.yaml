---
apiVersion: v1
kind: Service
metadata:
  name: media-proxy
  namespace: fluxer
spec:
  type: ClusterIP
  selector:
    app: media-proxy
  ports:
    - name: http
      port: 8080
      targetPort: 8080
