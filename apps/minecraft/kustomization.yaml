apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: minecraft
  namespace: minecraft
dependsOn:
- cert-manager-cluster-issuer
- traefik
resources:
- namespace.yaml
- helm-repository.yaml
- helm-release.yaml

patchesJson6902:
  - target:
      group: apps
      version: v1
      kind: StatefulSet 
      name: minecraft
    patch:
      - op: remove
        path: /spec/replicas
