apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cloudflare-ddns-cache
  namespace: cloudflare-ddns
spec:
  storageClass: csi-s3
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Ki
