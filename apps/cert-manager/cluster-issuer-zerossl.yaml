apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: zerossl-production
spec:
  acme:
    server: https://acme.zerossl.com/v2/DV90
    externalAccountBinding:
      keyID: eozH0I8_VE2ccobTDnNIow      
      keySecretRef:
        name: zerossl-eab
        key: hmac
    privateKeySecretRef:
      name: zerossl-production
    solvers:
    - http01:
        ingress:
          class: traefik
