apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teamcity
  namespace: teamcity
spec:
  storageClass: longhorn-replicated
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8Gi

