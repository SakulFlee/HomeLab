apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: synapse
  namespace: flux-system
  labels:
    reconcile.fluxcd.io/watch: Enabled
spec:
  interval: 15m
  timeout: 5m
  chart:
    spec:
      chart: synapse
      version: 1.1.x
      sourceRef:
        kind: HelmRepository
        name: synapse
      interval: 5m
  releaseName: matrix
  targetNamespace: matrix
  install:
    crds: CreateReplace
    remediation:
      retries: 3
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 3
  test:
    enable: true
  driftDetection:
    mode: enabled
    ignore:
      - paths: ["/spec/replicas"]
        target:
          kind: Deployment
  valuesFrom:
    - kind: Secret
      name: secret-synapse
      valuesKey: signingKeyB64
      targetPath: signingKeyB64
    - kind: Secret
      name: secret-synapse
      valuesKey: regSharedSecret
      targetPath: regSharedSecret
    - kind: Secret
      name: synapse-db-app
      valuesKey: dbname
      targetPath: postgres.database
    - kind: Secret
      name: synapse-db-app
      valuesKey: host
      targetPath: postgres.host
    - kind: Secret
      name: synapse-db-app
      valuesKey: password
      targetPath: postgres.password
    - kind: Secret
      name: synapse-db-app
      valuesKey: port
      targetPath: postgres.port
    - kind: Secret
      name: synapse-db-app
      valuesKey: user
      targetPath: postgres.user
  values:
    serverName: sakul-flee.de
    storageClass: longhorn-replicated
    image:
      pullPolicy: "Always"
      repository: "matrixdotorg/synapse"
      tag: "v1.147.0"
    service:
      port: 8008
      type: LoadBalancer
    ingress:
      enabled: true
      annotations:
        kubernetes.io/ingress.class: traefik
        cert-manager.io/cluster-issuer: zerossl-production
        traefik.ingress.kubernetes.io/router.entrypoints: websecure
      hosts:
        - host: matrix.sakul-flee.de
          paths: ["/"]
      tls:
        - secretName: matrix-tls
          hosts:
            - matrix.sakul-flee.de
