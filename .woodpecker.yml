steps:
  deploy:
    image: alpine/ansible
    environment:
      SSH_KEY:
        from_secret: SSH_KEY
    commands:
      - echo "$SSH_KEY" > private_key
      - chmod 0600 private_key
      - ssh -J root@192.168.178.200 -p 2222 root@10.0.0.101 hostname
      - ansible-playbook -i inventory.ini playbook.yml --private-key private_key
