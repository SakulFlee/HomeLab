# This manifest was generated by flux. DO NOT EDIT.
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  # Required so the apps/pvc-explorer/vendor/pvc-explorer submodule is included in
  # the artifact. NOTE: `flux bootstrap git` regenerates this file and drops this
  # field unless you pass --recurse-submodules (see README).
  recurseSubmodules: true
  secretRef:
    name: flux-system
  url: https://github.com/SakulFlee/HomeLab.git
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/homelab
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
